#!/usr/bin/ruby

require 'fileutils'

if ARGV.size == 0
  puts "Usage: ./theme new [name]"
  exit
else
  theme = ARGV[0]
  FileUtils.cp_r("config/default", "config/#{theme}")
  puts "== Generated config files: config/#{theme}"
  `bundle exec rake build`
end
